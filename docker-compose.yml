services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    environment:
      - DB_DIR=/data
    volumes:
      - db_data:/data
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

  test:
    build:
      context: .
      dockerfile: Dockerfile.backend
    environment:
      - DB_DIR=/tmp
    command: pytest -v
    profiles:
      - test

volumes:
  db_data:
